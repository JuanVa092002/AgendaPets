# Solo se aplica con SPRING_PROFILES_ACTIVE=dev. No usar en produccion.
jwt.secret=${JWT_SECRET:agendapets-dev-only-change-me-32chars}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000}
